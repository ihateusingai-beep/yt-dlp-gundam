# UTF-8
#
# ⚠️  MANUAL-PYINSTALLER FALLBACK ONLY ⚠️
#
# The canonical Windows build path is `yt_dlp_gundam.spec`, which auto-generates
# `build/version_info.txt` from `__version__` in main.py at build time — that
# file is the one shipped in the .exe.
#
# THIS file (repo root) is the fallback used when someone runs pyinstaller
# directly against `yt_dlp_gundam.py` *without* the spec, e.g.:
#     pyinstaller --onefile --version-file version_info.txt yt_dlp_gundam.py
#
# When bumping `__version__` in main.py:
#   1. The spec auto-bumps build/version_info.txt at next CI build
#   2. If you care about the manual fallback path, also bump filevers/prodvers
#      below to match. Otherwise leave stale — CI path is unaffected.
#
# For more details about fixed file info 'ffi' see:
# http://msdn.microsoft.com/en-us/library/ms646997.aspx
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items: (1, 2, 3, 4)
    # Set not needed items to zero 0. May not be 0.0.0.0.
    filevers=(0, 8, 4, 0),
    prodvers=(0, 8, 4, 0),
    # Contains a bitmask designed to only include the Attributes that are part of the version-Information structure.
    # Typically this is the combination of VS_FF_DEBUG, VS_FF_PRERELEASE, and VS_FF_PATCHED.
    mask=0x3F,
    # Contains the file attributes. They are part of the FILEATTRIBUTESTRUCT.
    flags=0x0,
    # The operating system for which this file was designed.
    # 0x4 - NT and there is no need to change it.
    OS=0x40004,
    # The general type of file.
    # 0x1 - the file is an application.
    fileType=0x1,
    # The function of the file.
    # 0x0 - the function is not defined for this fileType
    subtype=0x0,
    # Creation date and time stamp.
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'yt-dlp Gundam'),
            StringStruct(u'FileDescription', u'yt-dlp Gundam Dashboard'),
            StringStruct(u'FileVersion', u'0.8.4'),
            StringStruct(u'InternalName', u'yt_dlp_gundam'),
            StringStruct(u'OriginalFilename', u'yt_dlp_gundam.exe'),
            StringStruct(u'ProductName', u'yt-dlp Gundam Dashboard'),
            StringStruct(u'ProductVersion', u'0.8.4')
          ]
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)